test:
  override:
    - bundle exec rake generate

deployment:
  prod:
    branch: master
    commands:
      - aws s3 sync public/blog s3://tcias.co.uk/blog/ --acl public-read --cache-control 'max-age=604800' --region eu-west-1 --include="*" --exclude "*.jp*" --exclude "*.JP*" --exclude "*.pn*" --exclude "*.gif*"
      - aws s3 sync public/blog s3://tcias.co.uk/blog/ --acl public-read --cache-control 'max-age=31104000' --region eu-west-1 --exclude="*" --include "*.jp*" --include "*.JP*" --include "*.pn*" --include "*.gif*"
